# React + Vite
```shell
  npm create vite@latest gpt-app-starter
  # choose react
  # choose JavaScript
```
# install open ai dependency
```shell
  npm install openai
```
# configure your openai key in the OS env
